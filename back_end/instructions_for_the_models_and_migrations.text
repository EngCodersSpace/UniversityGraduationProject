
                       sequelize-cli

npx sequelize-cli db:migrate:undo:all --config config\database.json

npx sequelize-cli db:migrate --config config\database.json

npx sequelize-cli model:generate --name grade --attributes grad_id:integer,the_grade:integer

npx sequelize-cli db:seed:all --config config\database.json

npx sequelize-cli db:seed:undo:all --config config\database.json

npx sequelize-cli db:seed --seed 

npx sequelize-cli db:seed:undo --seed 

npx sequelize-cli seed:generate --name section

                    GIT

git commit -m "commit"

git add .

git status 

git show

git checkout (name of commit)

git push origin ModelsAndMigrations:ModelsAndMigrations

git switch ModelsAndMigrations







git 


git checkout BackEnd 

git fetch

git pull ||  git reset origin/BackEnd --hard 

git switch ModelsAndMigrations

git merge --squash BackEnd 

git commit --m "merge BackEnd to ModelsAndMigrations"